export class Contact {
    // Déclaration des variables
    id !: number;   // le ! permet de ne pas initialiser la variable
    contactName !: string;
    birthDate !: string;
    phone !: string;
    address !: string;
    // postcode !: string;
    city !: string;
    contactType !: string;
    photoUrl !: string;
}